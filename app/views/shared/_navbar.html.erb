<nav class="bg-white border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 py-3">
    <div class="flex flex-wrap items-center justify-between gap-4">
      <a href="<%= root_path %>" class="flex items-center gap-2">
        <span class="text-xl font-semibold text-gray-900"><%= t("app.title") %></span>
      </a>

      <div class="flex flex-wrap items-center gap-2">
        <%= link_to t("nav.clients"),
                    clients_path,
                    class: "px-3 py-2 rounded-lg " +
                      (current_page?(clients_path) ? "bg-primary-100 text-primary-700 font-semibold" : "text-gray-700 hover:text-primary-700") %>

        <%= link_to t("nav.invoices"),
                    invoices_path,
                    class: "px-3 py-2 rounded-lg " +
                      (current_page?(invoices_path) ? "bg-primary-100 text-primary-700 font-semibold" : "text-gray-700 hover:text-primary-700") %>

        <div class="relative inline-flex items-center rounded-lg bg-gray-200 p-1 transition-all duration-300" data-controller="language">
          <%= render "shared/language_switcher" %>
        </div>
      </div>
    </div>
  </div>
</nav>
